# MeowField AutoPiano v1.0.2 依赖包
# 核心依赖
ttkbootstrap>=1.10.1  # 现代化主题（可选，缺失将自动降级为 tkinter）

# 音频处理
mido>=1.3.0  # MIDI文件处理
pygame>=2.5.2  # 音频播放

# 输入控制
keyboard>=0.13.5  # 全局键盘控制

# 可选依赖（用于增强功能）
pillow>=9.0.0  # 图像处理（当前未强依赖）
pretty_midi>=0.1.28  # 高级MIDI处理（可选）

# 开发依赖
# 这些包在meowauto模块中已经包含，无需额外安装